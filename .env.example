# Copy to .env.local for the Next.js app.
# Where /api/* is proxied. Defaults to http://localhost:8085 during `npm run dev`.
# In production, set it to your deployed Spring Boot API; without it the app uses its offline interviewer.
HIREHEARSAL_API_URL=http://localhost:8085
